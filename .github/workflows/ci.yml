name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  regression:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Regression Suite
        shell: pwsh
        run: .\tests\Run-Regression.ps1

